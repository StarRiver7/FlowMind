// ============================================================
// FlowMind — Page Enum
// ============================================================
export enum PageEnum {
  BASE_LOGIN = '/login',
  BASE_HOME = '/dashboard/workspace',
  ERROR_PAGE = '/exception',
}